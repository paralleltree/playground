# rearrange

You can run

```
npm run build
```

or

```
yarn build
```

to bundle the application.

You can create symlink for executable with the built script by running:

```
yarn build && yarn link
```